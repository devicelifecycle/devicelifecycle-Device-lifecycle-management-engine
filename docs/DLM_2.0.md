# DLM 2.0 — Remaining Build Plan

**Source spec:** `BB VAR Outline structure revised 2.0.docx`
**Scope:** everything still to build to finish the white-label multi-tenant VAR
platform. **Completed foundations are intentionally excluded** (listed once
below so the boundary is clear). This is the tracked plan — keep it updated as
phases land.

**Timeline:** 4 months (≈16 weeks). A 3.5-month compression is noted per phase.
**Status legend:** ☐ not started · ◐ in progress · ☑ done · ❓ needs product clarification

---

## ⚠ Critical findings — full audit against the source outline (2026-08-17)
Read `BB VAR Outline structure revised 2.0.docx` line-by-line (all 436 lines
extracted and verified against live code, not assumed) on 2026-08-17. Three
findings change scope enough to flag before more Month 3/4 work starts:

1. **White-label leak: transactional emails and PDFs are NOT tenant-branded.**
   `email.service.ts`, `pdf.ts`, and `rve-pdf.ts` all read
   `process.env.NEXT_PUBLIC_APP_NAME` — one global constant — not the
   recipient order's tenant branding. Every order confirmation, quote,
   password-reset, shipment-status email, and generated PDF says "Byte-Back"
   today regardless of which VAR the order belongs to. This directly
   contradicts the outline's guiding principle: *"An End Customer should never
   know they are using software owned by your company unless you
   intentionally expose that information."* **This is the single biggest
   white-label gap found — bigger than anything previously tracked.** New line
   item added to Month 1 below.
2. **~~Billing "Option A vs B"~~ — RESOLVED 2026-09-22: BOTH, chosen per VAR.** (Original finding kept below for context.)
   **Built:** `settings.billingMode` on each tenant — `external` (Option A, the DEFAULT so nothing changes for anyone) or `in_platform` (Option B). Option B adds `customer_invoices` / `_line_items` / `_payments` with per-tenant, per-year atomic numbering, `GET/POST /api/var/customer-invoices` + `[id]` (send / void / record payment / record refund), a `/var/customer-invoices` page, and a billing-mode selector on the VAR console. Tax comes from the CUSTOMER's billing address through the same engine the order PDFs use, so an invoice can't disagree with the order documents. **Payment collection is deliberately absent** — the gateway decision is still deferred, so payments are recorded by hand, exactly like the BB→VAR invoices; an invoice states what is owed without implying we can charge a card. Separate tables from `invoices` on purpose: that one means "BB billing the VAR its commission", and merging the two is how a VAR's own commission bill would surface in their customer-billing screen. **Verified live (10/10, `tests/e2e/customer-invoicing.spec.ts`):** Option A refuses with 409 (not 403 — nothing is forbidden, the mode is just off); switching to B turns it on; two closed orders → subtotal 750.50, 13% HST (ON) 97.57, total 848.07; re-billing the same orders refused (unique index on tenant+order); payment above balance refused; refund with nothing paid refused (the exact unbounded-refund bug the BB billing shipped with); part payment → 448.07 outstanding; settled → paid; another tenant cannot read the invoice.

   *Original finding:* **Billing "Option A vs B" — the client's answer doesn't resolve it.** The
   outline (VAR Billing) frames Option A as *"VAR processes final bill or pays
   customer externally using their proprietary billing system"* — i.e. the VAR
   already bills the customer directly under Option A too, just outside our
   platform. Option B means Byte-Back builds in-platform invoicing FOR the
   VAR to bill their customer (invoices, payment collection, coupons, tax,
   outstanding-balance tracking — all end-customer-facing). The client's
   answer ("the reseller bills the end customer directly") is consistent with
   *either* option. **Needs one direct clarifying question before Month 3
   billing scope is locked** — see Open decisions.
3. **No granular permission-string RBAC model.** The outline's own "Enterprise
   Best Practices" section explicitly asks for `customer.create`,
   `user.delete`, `reports.view`-style permissions assignable to roles, not
   hardcoded role checks — and "Assign permissions" appears as a required
   capability at all three tiers (BB User Management, VAR User Management,
   Customer User Management). The live codebase has **none of this** — every
   route gates on a flat `role === 'admin'` (or similar) check. Roles work;
   granular per-user permission assignment does not exist anywhere. This is an
   architectural gap, not a small addition — flagged as its own line in
   Month 4 below rather than folded into existing "auth enforcement" bullet.

---

## Already shipped — OUT OF SCOPE (do not re-plan)
Foundations, engines, APIs, migrations, and tests that are already built,
verified, and on `main` (frontend currently behind *Coming Soon*):

- Multi-tenant core (`tenants`, `tenant_id`, tenant-aware auth) + tenant-isolation RLS **applied and verified enforcing on prod (2026-09-14)**
- RBAC (permissions/roles/user_roles) + delegated VAR role templates (Appendix A)
- Commission engine (platform commission + product margin + holdback) + commission reporting (O(1))
- White-label **branding** resolver + editor; per-VAR feature flags; licensing/quota model
- Billing: invoices + atomic numbering + subscription plans + status transitions + credits
- Platform analytics (MRR/ARR/counts); Support ticketing; RVE engine + admin quote calculator
- Canadian tax engine; host→tenant **resolver** lib; **impersonation audit log**
  - ⚠ Correction: the "per-tenant security config (password/MFA/IP)" once listed here was **never real**. `settings.security` was accepted, validated, stored and returned while nothing read it for enforcement, so an operator could set an IP allowlist or require-MFA and be no more protected. That surface was removed 2026-09-12. The controls that ARE enforced live on tenant **branding**: `allowedIps` (checked in `requireAuth`), `passwordPolicy` (both password-set flows — client-side only, see Month 4), `requireMfa` (**enforced server-side in `requireAuth` since 2026-09-18** — was a nudge banner only before that). `settings.integrations` (SMTP/SMS/payment/SSO provider fields) was the same kind of decoy and was removed 2026-09-14.
- Blue rebrand + landing; order-flow fixes (storage in review, edit-quote add-item+notes, CPO grade, CAD/USD)

> These are done. The plan below is **only the remaining wiring, depth, and net-new work.**

---

## Month 1 — Make tenancy real (runtime isolation)
*Turn the built foundations into a live multi-tenant system.*

- ☑ Per-request tenant context via `getServerTenant()` (server-side, React-cached; platform host = no-DB fast path)
- ☑ Runtime **per-tenant branding/theme** injection in the root layout (`tenantBrandingStyle`, `:root:root` both themes; no-op for platform)
- ☑ **Child rows inherit `tenant_id` from their order — DB trigger, applied + proved 2026-09-19.** Auditing the two order-creation paths found that `OrderService.createOrder` stamped the ORDER's tenant but inserted `order_items` with none (→ platform default), and `/api/orders/upload-csv` — the primary bulk path — inserted the ORDER itself with no tenant and with none of the module/transaction gates `POST /api/orders` had. Under the restrictive RLS a VAR user would have seen their order with no items, or no order at all; invisible today only because a single tenant exists. Fix at the layer that can't be bypassed: `inherit_tenant_from_order()` BEFORE INSERT on all 8 child tables that reference an order (`order_items`, `order_timeline`, `order_exceptions`, `shipments`, `imei_records`, `triage_results`, `vendor_bids`, `sla_breaches`) — the parent's tenant always wins. Proved live in a rolled-back transaction (item inherited the VAR tenant, nothing left behind); 0 pre-existing mismatches. The CSV route now stamps the order's tenant and runs the same `orderCreationGate` as `POST /api/orders` (module on? monthly transactions under cap? fails closed).
- ☑ **Apply + verify tenant-scoped RLS — APPLIED AND VERIFIED ENFORCING 2026-09-14.** The migration turned out to have been applied back in August, but under a timestamp it shared with another file, which is what jammed `db push` for weeks (see the migration-hygiene note below). It has now been re-asserted under a unique version and, more importantly, **verified against the live database rather than assumed**: reading all 23 tenant-scoped tables with the public anon key returns 0 rows on every one, while the service role sees real data. That is the isolation property actually holding, not just DDL that ran.
- ☑ VAR provisioning: **data-scoping done** (creator's tenant stamped on users/customers/orders via `nonPlatformTenantId`); `var_*` role system shipped in Month 2; **the missing first step — creating a VAR's first `var_entity_admin` — shipped 2026-09-18.** Until then no UI could do it: `/var/team` limits itself to regional managers and sales reps (an entity admin must not mint peers) and the Users page only knows the six core roles, so a new VAR had nobody who could log in without a hand-inserted row. Now: `GET/POST /api/admin/tenants/{id}/admins` (platform admin only, refuses the platform tenant and inactive VARs) + a "VAR administrators" card on the VAR page that lists them and invites one, reusing `UserProvisioningService` end to end (welcome email under the VAR's branding; temp password surfaced on screen when no email can be sent, never discarded). Verified by tsc + prod build; the provisioning service underneath is the one `/var/team` already exercises in production.
- ◐ **Feature flags + quotas.** Quotas are genuinely enforced on every create path (`customers`, `customers/bulk`, `orders`, `users` — all via `quotaBlockMessage`, fail-closed). Feature flags were NOT: this line read ☑ for weeks while exactly ONE route (`/api/orders`, for `trade_in`/`cpo`) checked a flag, so an admin could disable a module, watch the UI confirm the save, and the module kept working. Corrected 2026-09-14 with a shared `featureGate()` (fails closed, no-op for an unconfigured tenant). **Enforced now: 5 of 12** — `trade_in`, `cpo` (orders), `rve` (both RVE quote routes), `knowledge_base` (`/api/kb`), `api_access` (`/api/var/api-keys` **and every `/api/v1` call**, verified live 2026-09-18: switching it off returns 403 to existing keys immediately). **Still unenforced: 7** — `billing`, `reporting`, `notifications`, `chat`, `impersonation`, `vendor_auction`, `sso`. `DEFAULT_FEATURES` was also corrected first: `knowledge_base`/`chat`/`impersonation` defaulted to false while being live, so enforcing as-written would have switched shipped modules off for everyone. API-call and storage metering remain deferred (no runtime source).
- ◐ Custom-domain onboarding — resolver + runtime branding already support subdomains + custom domains; operational checklist at [[DLM_2.0_Custom_Domain_Onboarding]] (DNS + Vercel + Resend). Per-VAR lighting-up is operational, done at launch.
- ☑ **Per-tenant branding on outbound emails.** `resolveTenantBrandLabel()` (`src/lib/tenant-brand-label.ts`) added 2026-08-18, wired into `emailShell()` and the two customer-facing sends a VAR's own customer actually sees (order confirmation, shipment status). The other 7 email methods (welcome/password-reset/OTP/SLA-reminder/recurring-reminder — internal + account-security mail) and the two PDF generators (`pdf.ts`, `rve-pdf.ts`) still use the global `APP_NAME` — smaller follow-up, not the highest-value target. Completed 2026-08-23 — ALL 9 email methods now accept `tenantId` and resolve VAR branding (order-status, welcome, password-reset, OTP, password-change, SLA-reminder, recurring-reminder joined the original two); both PDF generators (`pdf.ts` order PDF + order-history PDF, `rve-pdf.ts`) take an optional `brandName` resolved from the record's tenant at all 4 call routes; every `[Byte-Back]` SMS literal (18 sites across notification/shipment/order/vendor/cron routes) now interpolates the resolved tenant brand. Callers thread `tenantId` from the record (order/user/profile), not the request host. **Regression found + fixed 2026-09-10:** the 3 routes that email a PDF as an attachment (`send-quote-email`, `rve/quote`, `rve/quote/send`) all resolved the tenant brand for the PDF/Excel/HTML content but never passed it into `EmailService.sendEmailWithAttachments`, so the email's actual From: name/address still fell back to the global identity — the content was branded, the envelope wasn't. Fixed by threading `brand` through to `getFromEmail()` at all 3 call sites.
- ☑ **Storage quota + API-call + AI-token metering per tenant — shipped 2026-09-19.** `20260919000000_tenant_usage_metering.sql`: `tenant_usage_daily` (one row per tenant per day, atomic upsert RPC, service-role only — anon/authenticated execution verified refused live) for API calls and AI tokens; storage is *measured* from `storage.objects` by a SECURITY DEFINER RPC joined through `organizations.tenant_id`, never counted, so upserts/deletes can't drift it (live: platform tenant = 2 objects / 42,384 bytes). Counted at: every authenticated `/api/v1` call (after the gates, so a refused request is never metered) and every Groq completion in `/api/chat` (each tool round + final, charged to the user's tenant). **Enforced:** `license.apiCallsPerMonth` in `authorizeV1` → 429 `quota_exceeded` (fails closed: 503 if the rollup can't be read); `license.storageMb` on order-file upload → 413 against the measured total. Platform report's Storage tile and License table now show real bytes / API calls MTD / AI tokens MTD, with `null` → "unavailable" on a failed source, never a fake 0. End-to-end verified against the production build: 12 calls metered for a temp tenant, cap set to 12 → 429, raised → 200. `transactionsPerMonth` was already enforced (a transaction = an order created in the UTC month, `POST /api/orders`) — the earlier note that it had no runtime source was wrong; it is now also enforced on the CSV upload path via the shared `orderCreationGate`.

*3.5-mo: keep RLS + proxy wiring; fast-track domain onboarding to Month 4.*

---

## Month 2 — Consoles (VAR Admin, delegated, End Customer)
*Un-pause and complete the three-tier consoles from the outline.*

- ☑ **VAR Admin console (backend):** delegated roles are now first-class (auth + routing); customer-management APIs shipped — `manage` (suspend/reactivate/assign/move), delegation-scoped export, chunked bulk import; search already existed. Console UI shipped 2026-08-23: `/var/customers` management page (search, status/plan filters, suspend/reactivate, assign_plan incl. inherit-null, region move, bulk import dialog, filtered export), `/api/var/plans` allowed-plans read, Features page `/var/features` with ceiling-respecting toggles (`applyVarToggles` wired into tenantLimits so every consumer computes effective = ceiling AND var override).
- ☑ **VAR user & rep management** (add/disable reps, reset passwords, region assignment) — shipped 2026-08-18. `ALTER TYPE user_role ADD VALUE` migration applied to prod (dry-run verified first); `GET/POST /api/var/team` + `POST /api/var/team/[id]` (disable/reactivate/reset_password/reassign_region); new `canManageVarTeamMember`/`resolveTargetTenant` in `delegation.ts` (15 unit tests); UI at `/var/team`. "Create customer users" (a VAR provisioning a *login* for one of their own end customers, not a rep) is a smaller, distinct follow-up — not included in this pass.
- ☑ **Delegated N-level roles** enforcement (Appendix A: Entity → Regional Manager → Sales Rep scoping) wired into customer listings/export/management **and now team management**; roll-up reporting still to do — Roll-up reporting shipped 2026-08-23: pure aggregator `src/lib/var-rollup.ts` (7 unit tests, fixture typo fixed), scoped API `GET /api/var/reports` (entity/regional/rep scoping via delegation, region+rep filters, bounded fetches), UI `src/app/(dashboard)/var/reports/page.tsx` (by-rep + by-region tables, summary cards, unassigned-customer callout).
- ☑ **End Customer console** (marked ◐ until 2026-09-18; verified complete and now live — page, API and the un-gating all confirmed present): company profile (backend + UI shipped 2026-08-17 — `customer_assets`/`company_profile` schema + APIs already existed from an earlier pass, only the page was missing), locations, departments, contacts, business hours — all in the shipped page
- ☑ Customer **device/asset register** (register/assign/retire/move/audit) + own reports/exports (marked ◐ until 2026-09-18; all four API routes, the register page, the bulk-import route and the reports page verified present and un-gated) — register/assign/retire/move shipped 2026-08-17 (backend already existed). **Audit log + own reports/exports shipped 2026-08-23**: `customer_asset_events` migration (append-only, RLS, `registered/assigned/unassigned/retired/restored/moved/updated` check constraint), `GET /api/customer/assets/[id]/events` (tenant-scoped, 404s rather than leaks existence), a "History" dialog on each asset row, and `GET /api/customers/me/reports` + `/customer/reports` page (order/asset roll-up counters, recent-orders table, recent-assets table, reuses existing CSV/PDF export). *Found the base `GET/POST/PATCH /api/customer/assets` route file empty in the working tree — a prior session's file-write attempts had failed mid-edit and left behind five broken scratch scripts (`write_route.js`/`write_final.ps1`/etc.) instead of the real file; reconstructed it from the most complete draft, fixed a duplicated-return bug and a details-shape mismatch between the API and the History dialog's renderer, verified with `tsc` + a clean prod build, then deleted the scratch scripts.* Asset AUDIT history shipped 2026-08-23 (customer_asset_events migration, append-only RLS, 4 instrumentation points + batch events in bulk import, lazy History dialog); asset BULK IMPORT shipped 2026-08-23 (/api/customer/assets/bulk: row validation, in-payload + DB duplicate detection, 500-row cap, per-row failure report with downloadable CSV); customer own Reports page shipped 2026-08-23 (/customer/reports + counters API + PDF/CSV exports wired to existing endpoints).
- ☑ **NEW: per-customer license/plan assignment.** Outline lists "Assign licenses, Assign plans" under VAR Customer Management — read as per-customer, not per-tenant. Today licensing/plans resolve at the **tenant** level only (`tenant-limits.ts`); there is no way for a VAR to give one customer more seats/storage than another. Shipped 2026-08-23: `customers.plan_id` FK→subscription_plans (NULL=inherit tenant plan), pure resolver `src/lib/customer-limits.ts` (reuses resolveLicense; 6 unit tests), assign/unassign via manage action 'assign_plan', enforcement at the users-route customer-user quota using resolveCustomerLimits (fail-closed preserved), plan visible in customer detail + console column/filter.
- ☑ **NEW: VAR-facing feature-flag toggle UI.** Outline's "VAR Product Configuration" lists "Enable features / Disable features" as a *VAR-level* capability (distinct from BB Admin's "Configure feature availability," which sets the ceiling). Feature-flag infrastructure exists (`features.ts`) but there is no VAR-facing UI to toggle which of their *allowed* features are actually turned on — `var/page.tsx` today is read-only (branding + margin + invoices, no toggles). Shipped 2026-08-23 — covered by the VAR Admin console note above (Features page `/var/features`, ceiling-respecting `applyVarToggles`).
- ☑ **NEW: "archive" as a distinct customer state — resolved 2026-08-23: NOT a distinct state; one binary flag is the intended semantics.** Evidence: suspend (`is_active=false`) already hides the customer from default lists and exports (`CustomerService.getCustomers` defaults `is_active=true`, `customer.service.ts:36,43`; both `/api/customers` and `/api/customers/export` go through it), blocks new orders (`orders/upload-csv/route.ts:400` rejects inactive customers), blocks portal logins (login gates on `users.is_active` in `proxy.ts:159`, and deactivation releases the portal-login email via `releasePortalLoginEmail`, `customer.service.ts:215-241`), and is restorable (`reactivate` action in `customers/[id]/manage/route.ts:64`). No third-state precedent exists anywhere in the schema or codebase (no `archived_at`/`deleted_at` column in any migration; the only soft-delete precedent reuses a binary flag — vendors restore by flipping `is_active`, `vendors/deleted/page.tsx`). An archive state would duplicate every behavior above under a different label. Known follow-up (orthogonal to this decision): quota counts include inactive customers (`api/customers/route.ts:151`, `api/customers/bulk/route.ts:69`, `api/var/usage/route.ts:24` have no `is_active` filter), so suspending does not yet free a seat — if seat-freeing is wanted, filter those count queries rather than adding a third state. Resolved 2026-08-23 by evidence review: suspension already covers every archive semantic (hidden lists/exports, login blocked via users.is_active + portal-email release, order creation blocked, restorable); no third state added. Related fix: customers-quota counts now filter is_active=true at all three count sites so suspension frees a seat.

> **Plan paused after M2.2 at user request (2026-08-07), resumed 2026-08-17.**
> Landed so far: M2.4 (Company Profile) ☑, M2.5 (Asset Register UI) ☑, M2.3
> (VAR rep management) ☑, M2.3b (roll-up reporting) ☑ 2026-08-23, customer-asset audit log + own reports/exports ☑ 2026-08-23, VAR Admin console UI (`/var/customers` + `/var/features`) ☑ 2026-08-23, per-customer license/plan assignment ☑ 2026-08-23.
> **2026-09-12 manual QA gate: done.** A full UI-to-API wiring audit (not just file-existence/tsc) found and fixed real bugs: cross-customer IDOR across every `/api/customer/*` + tickets + reminders route (any customer could read/write another customer's data — see Security section below), `useMyCustomer` gating on stored role instead of active role (hung 3 customer pages on a permanent spinner for dual-role accounts), a Radix `SelectItem value=""` crash in the Assign-Plan dialog, VAR team temp passwords generated then silently discarded (permanent rep lock-out for login-ID accounts), and a `customer-self-scope` helper that broke on any org with more than one `customers` row. All fixed, tested, and pushed. The lower-severity items were closed the same day too: the region field now suggests exact spellings (and the API names the expected region when it rejects), `unassignedCustomerCount` is now actually reachable (customers with no rep were never fetched, so the callout could never fire), report filter options come from an unfiltered fetch, row caps are flagged instead of silently truncating, the CSV importer sends only the rows it promised and maps failures back to real line numbers, 403s read as "not permitted" rather than "nothing here", and `reassign_region` — implemented server-side with no UI at all — got its control.

> **Rollout status (re-checked against the tree 2026-09-18):** Month 1 and Month 2 pages are **live** — un-gated in `76d958d` ("un-gate Month 1 + Month 2"). The earlier note that Month 2 still rendered `<ComingSoon />` is stale and removed.

*3.5-mo: ship VAR Admin + delegated roles; End Customer console trimmed to profile + reports.*

---

## Month 3 — Commerce depth (Billing, Pricing, RVE, Reporting)
*Complete the money-movement + reporting surfaces, then un-pause them.*

- ☑ **Billing reconciliation** shipped 2026-08-25 — **Option-A-compatible**, sidesteps the A/B ambiguity: this is BB billing the VAR its commission (already-scoped, unambiguous), not the VAR billing its own end customer (the open A/B question). `src/lib/billing-reconcile.ts` (pure, tested) + `POST /api/admin/billing/reconcile` (admin-only, tenant+period idempotency guard against double-billing, 92-day window cap, atomic invoice numbering via `next_invoice_seq`) + a "Reconcile period" form on `/admin/billing`.
- ◐ **PO (Trade-In) vs Invoice (CPO)** flows; credits/refunds/payment history; tax on invoices; subscription-plan limit enforcement — `invoice_payments` table + RLS + `/api/admin/billing/[id]/payments` route already exist (found in the 2026-08-17 audit, migration dated 2026-08-13, comment tag "Wk10"); needs a UI pass to confirm it's fully wired
- ☑ **RVE productionization** shipped 2026-08-25 — `src/lib/rve-quote.ts` resolves each line's base value from the pricing engine (manual override still allowed) and projects it off the admin-configured depreciation rate (`pricing_settings.cpo_depreciation_rate`, same table both routes share); `POST /api/rve/quote/send` emails the branded PDF to an explicit recipient (mirrors the order send-quote-email pattern — escaped HTML, tenant-branded).
- ◐ **BB Admin reporting suite** shipped 2026-08-25, honestly scoped: `src/lib/operations-metrics.ts` + `GET /api/admin/reports/operations`, wired into `/admin/reports/platform` — usage (orders MoM), active/inactive (users, customers), per-VAR license table all real; storage and API-call metering were surfaced as "Not yet metered" until 2026-09-19 — **now real** (see Month 1); **failed-login security metric intentionally omitted** — auth happens client-side against Supabase directly, so there's no server path that could record an attempt (documented in the route).
- ☑ **Un-pause** Pricing, Commission, Plans, Billing, Reports, VARs, Platform Analytics — code-complete 2026-08-25, re-gated by the "Month 1 + 2 only" rollout decision (`632110c`), and **un-gated for good on 2026-09-18 on the client's explicit go-ahead**: all 11 remaining Coming Soon pages (`/admin/billing`, `/admin/commission`, `/admin/plans`, `/admin/rve`, `/admin/reports/commission`, `/admin/reports/platform`, `/admin/roles`, `/admin/knowledge-base`, `/support/knowledge-base`, `/var/api-keys`, `/var/communications`) now render their finished implementations. `grep -rl ComingSoon src/app` returns nothing. Every one of these passed the Month 3 QA gate and today's settings-parity sweep before the gate came off.
- ☑ **Confirmed 2026-09-10: commission is never itemized to the VAR.** Verified directly against code — neither `pdf.ts` nor `rve-pdf.ts` (the two PDF generators) references commission anywhere in their output. Closed.

> **Month 3 core build is essentially done as of 2026-08-25** — billing reconciliation, RVE sendable quotes, the admin un-pause, and the reporting suite all landed. What's left: the commission-itemization QA check above, a UI confirmation pass on payments/refunds, and the manual signoff on the un-paused admin pages.
>
> **2026-09-12 manual QA gate: done, with real bugs found and fixed.** Worst: the RVE admin page priced its on-screen preview off the hardcoded default depreciation curve while the emailed PDF priced off the admin-configured rate — at defaults, a $1,000 device showed $300 on screen and $614 in the customer's PDF. Also fixed: unbounded invoice refunds (could drive net-paid negative with no cap), a billing-reconcile idempotency check that discarded its own error and could let a duplicate invoice through, a UI/API status-list mismatch between the two reconcile routes (already fixed 2026-09-10, re-verified consistent), an unvalidated IP-allowlist field that could self-lock a whole tenant out on a typo, and a commission-settings page that could silently overwrite real rates with defaults on a failed load. **The remaining feature gaps were then closed the same day.** Plans gained edit / retire / delete (`PATCH`+`DELETE /api/admin/plans/[id]`; the slug is immutable because `tenants.plan` joins on it and renaming would orphan every tenant on that plan, and delete refuses with a count while any VAR is still on it). The tenant detail page gained the subscription-plan selector that never existed, so MRR/ARR is no longer structurally $0.00 — note enforcement still reads `settings.license`/`settings.features` and *not* the plan, so picking a plan fills those fields in visibly rather than implying a link that doesn't exist. Billing can now produce a single invoice carrying both a subscription fee and commission lines (fee added to the reconcile-period form, and the previously caller-less fee-aware `[id]/reconcile` route wired to a per-draft "Rebuild lines" action). Also fixed: an RVE lookup race that could email a quote at a stale price, `var/reports` filters that ate their own options, silent 5000-row truncation (now flagged, with the orders fetch chunked so a large `.in()` can't fail the whole report), an unenforced `settings.security` block removed from the tenant API, and the per-VAR `COUNT` N+1 in the operations report replaced with one grouped aggregate.

*3.5-mo: billing reconciliation + RVE sendable are must-haves; defer deep BB reporting dashboards to launch buffer.*

---

## Month 4 — Security, integrations, support, launch
*Harden, integrate, and go live.*

- ◐ **Auth enforcement** — landed 2026-08-27 (concurrent work stream, verified against live code): **IP allowlist is genuinely enforced end-to-end** — `tenants.branding.allowedIps` is settable from `admin/tenants/[id]`, and `requireAuth()` rejects non-matching requests. **Password policy correction (2026-09-10): the "zero callers" claim above was stale/wrong** — `validatePassword()` was already called from the profile change-password flow. What audit did find real: `reset-password/page.tsx` only checked a flat 8-char regex and silently skipped the tenant's configured policy on that path. Fixed — `passwordPolicy` now threads through `AuthBrandingContext` so both password-set flows enforce the same tenant policy. Still true: both checks are client-side only; there is no server-side re-validation before Supabase accepts the new password. **MFA enforcement shipped 2026-09-18** (see the Month 4 status note below). SSO is still ☐ not started.
- ◐ **Granular permission-string RBAC** — the *foundation* landed 2026-08-27: `src/lib/permissions.ts` (permission catalog + role→permission map), `requirePermission()` guard, `useCan()` client hook, `GET /api/admin/rbac/permissions` (read-only catalog for the admin Roles page). **But adoption is essentially zero** — `requirePermission()` is used by exactly one route (the catalog endpoint guarding itself) and `useCan()` isn't called from any UI component *(first UI adoption 2026-09-18: the VAR margin editor gates on `commission.var_margins`; still one component)*. Every other route in the app still hardcodes a role check. Don't count this as done — it's scaffolding a future migration hasn't happened yet.
- ☑ **Secure impersonation** — landed 2026-08-27: `impersonation_log`-backed session swap in `requireAuth()` (re-checks `effectiveRole === 'admin'` at swap time, not just historical actor identity — a demoted admin loses the swap immediately), `POST/GET/PATCH /api/admin/impersonation`, persistent `ImpersonationBanner` wired into the dashboard layout. Found and fixed two real gaps during review: the "can't impersonate another admin" rule was UI-only (no server check) and the swap didn't re-verify current admin status.
- ◐ **Integrations** — API key **management** landed 2026-08-27 (`GET/POST /api/var/api-keys`, `DELETE .../[id]`, hashed storage, VAR + admin UI at `/var/api-keys`) — found and fixed a real auth gap (GET had no role check) and a stale route-params bug during review. **But there is no public API yet** — `requireApiKey()` (the bearer-token auth guard) is fully built but **guards zero actual routes**; a generated key currently unlocks nothing. SMS/SMTP sender config landed as `/var/communications` (emailFromName/emailFromAddress/smsSenderId on tenant branding). ⚠ **The claim that this "isn't consumed by the send path" is STALE — corrected 2026-09-18.** `getFromEmail()` reads `brand.emailFromAddress` and `brand.emailFromName` first and only then falls back to the env vars, and `smsSenderId` is consumed in the SMS path; both verified in `email.service.ts`. What WAS genuinely broken (and is fixed) is that the three attachment-email routes never passed the resolved brand in, so the envelope fell back to the global identity even though the content was branded. Payment gateway still ☐, deferred to the client face-to-face per Open decisions.
  - Separately: `settings.integrations` (smtpHost/smtpPort/smtpUser/smsProvider/smsFrom/paymentProvider/ssoProvider/ssoEntityId) was accepted and stored by the tenant API while nothing read ANY of it — configuring a tenant's own SMTP server or SSO provider reported success and changed nothing. Removed 2026-09-14; real per-tenant sender identity lives on branding, above.
- ◐ **White-label depth:** logo image upload shipped (2026-08-25). **The "still missing" list here was stale and is corrected 2026-09-18 — four of its five items now exist and are verified consumed:**
  - ☑ **KB links** — `settings.whitelabel.knowledgeBaseUrl` is returned by `GET /api/kb` and surfaced as a "Browse our full help centre" link on the reader page.
  - ☑ **Privacy policy customization** — `/privacy-policy` is now a server component that redirects to the tenant's own `privacyPolicyUrl` when set (only http(s) values can be stored, so a stored value can't become a `javascript:` redirect); otherwise the platform policy renders.
  - ☑ **Secondary brand colour** — the field existed but emitted a `--brand-secondary` CSS var that NOTHING read, while the admin colour picker showed a live preview off the object directly, so the preview claimed an effect the app never applied. It now drives the outbound email header gradient (as inlined hex — email clients strip CSS custom properties), covered by tests.
  - ☑ **Support phone** — `branding.supportPhone` exists, is validated, and renders on the login page.
  - ☐ **Custom login page** — still the one genuine gap. The login page IS tenant-branded (logo, colours, tagline, support phone, help URL), but there is no per-VAR layout/content customization beyond that.
  - ⚠ Also corrected: "email/notification templates wired into live sends (2026-08-23)" was **false**. `resolveWhiteLabel` had zero callers outside its own module and the admin editor, so a VAR could write a custom quote subject/intro/signature, save it, and every field was ignored. Genuinely wired 2026-09-13 into the quote-email path, per-field (treating the three as one unit meant customizing only the signature also blanked the order number out of the subject).
- ◐ **Support depth** — landed 2026-08-27: knowledge base (`kb_articles` table, full CRUD API, `/admin/knowledge-base` authoring UI, `/support/knowledge-base` reader UI) and ticket SLAs (`tickets.sla_due_at` set on creation per-priority, on_track/breached computed in `src/lib/tickets.ts`, shown on the tickets page) are both genuinely wired end-to-end. Chat is ✅ — **resolved 2026-09-18: the AI `ChatAssistant` is what "Chat" in the outline means**, so there is no human live-chat widget to build. Escalation from chat to a ticket is the path to a human.
- ☐ Vendor auction / quote-to-vendor (open question in the outline — confirm scope). **Client answer 2026-08-17: not initially, discuss complexity for the future.** Complexity note for that discussion: single-tenant vendor bidding already works today (Byte-Back's own CPO vendor pool); making it *per-VAR* (each VAR's own private vendor pool, one VAR's vendors never seeing another VAR's deals) needs vendor records to become tenant-scoped, which they are not today — a real but bounded lift, not a rebuild.
- ◐ **Launch:** un-pause of all *Coming Soon* features ☑ 2026-09-18 (nothing gated remains), and each un-gated page verified to actually render in a browser 2026-09-22 (18/18, `tests/e2e/ungated-pages.spec.ts`); data migration and per-VAR custom-domain lighting-up are operational steps at the time a real VAR onboards (runbook: [[DLM_2.0_Custom_Domain_Onboarding]]); manual click-through signoff by the client is the remaining QA step.

> **Month 4 status — re-verified against running code and the live database, 2026-09-18.**
>
> **Genuinely done end-to-end:** impersonation (audited session swap, admin-on-admin blocked server-side), IP allowlist (enforced in `requireAuth`, now with format validation so a typo can't silently lock a tenant out), ticket SLAs (`sla_due_at` + `resolved_at`, stamped only on the transition INTO a resolved state so a late close no longer flips "met" to "breached"), knowledge base (table applied to prod 2026-09-14 — it had no table at all before that, so this was documented as "wired end-to-end" while being non-functional in production), per-tenant sender identity, and white-label depth per the bullet above.
>
> **Built but still unwired — do NOT count as done:**
> - ~~**Public API** — `requireApiKey()` is complete and guards zero routes.~~ **☑ SHIPPED 2026-09-18** as a versioned, read-only, tenant-scoped surface: `GET /api/v1/{me, orders, orders/{id}, customers, customers/{id}, devices}` (product decision: mirror the VAR console read-only first). Every route goes through one helper (`src/lib/public-api`) that does bearer auth → per-key rate limit → `api_access` feature gate → scope check, and `tenantQuery()` is the only way a route can start a query — it pins `tenant_id` before the route sees the builder, which matters because `requireApiKey()` hands back the service-role client (RLS bypassed). Field exposure is by explicit whitelist (no `*`): internal notes, pricing metadata, commission, vendor ids, credit limits, payment terms never leave. **Verified live, not assumed:** the exact SELECT strings were run against production (caught that `orders.direction` does not exist — the column is `order_direction`, and the TS `Order.direction` field is stale), then a 32-check end-to-end run against the production build with a temporary tenant: auth matrix (401/403/scope), cross-tenant ids 404 with no existence leak, filters, validation, the feature-gate switching keys off, revocation, and positive reads of a temp customer + asset — all cleaned up after (DB confirmed back to 1 tenant / 0 keys). Reference: `docs/PUBLIC_API_v1.md`; the in-product endpoint table replaced the "nothing to call yet" warning on `/var/api-keys`. Two real bugs found on the way, both from the same cause — a supabase-js builder only sends when `.then()` is called, so a bare `void builder` never runs: `api_keys.last_used_at` was never stamped (keys in daily use showed "never"), and **`users.last_login_at` was NULL for all 23 live users** despite daily logins — the value every team page displays. Both fixed. Also: `isValidUUID()` demanded RFC-4122 v1–v5, stricter than Postgres, so hand-seeded ids (`10000000-0000-…-0001`) 400'd across the 20 routes that guard with it; relaxed to Postgres semantics.
> - **Granular RBAC** — `requirePermission()` guards one route (the catalog endpoint guarding itself); `useCan()` has no UI callers. The `permissions` and `role_permissions` tables are seeded and read by nothing. Still scaffolding.
> - ~~**`PATCH /api/var/margins`** — VAR self-service margin control with no front end.~~ **☑ 2026-09-18:** the VAR console's Margin model card now carries an editor for the VAR's own corp/rep margins (percent typed as 12.5, stored as the 0.125 fraction the engine expects; fixed typed in dollars). Rendered only for roles holding `commission.var_margins` on their *active* role — matching the server gate exactly, so the control never renders and then 403s — and never for the platform tenant. Also tightened both margin APIs: a percent margin above 1 (a >100% take, always a units slip) is now refused server-side instead of silently mispricing every deal. Least-privilege correction, same day: `var_regional_manager` had `commission.var_margins` in the permission map, which with this UI would have let a regional manager rewrite the whole tenant's margin model. The outline places the margin inputs under the VAR Administrator and scopes the regional tier to one region's reps and customers (Appendix A), so the grant was removed (they keep `commission.view`); the server gate and the UI both read the map, so both closed together. Trivially reversible if product wants it back.
>
> **Not started:** SSO (parked until the client names the IdP).
>
> **Data retention — ☑ COMPLETE 2026-09-22. Stage 1 (policy + dry run) 2026-09-18; stage 2 (live execution) 2026-09-22 on the client's instruction ("no dry run, its a live system which have live data flow").** Stage 1 unchanged: per-tenant `settings.retention` (days per class, floor 30 / ceiling 3650, null = forever) on the VAR page, and `GET /api/admin/retention[?days=N]` still answers "what would a run remove". Stage 2 deletes for real: nightly cron `/api/cron/retention` at 03:00 UTC, plus a Run-now control on `/admin/retention` that requires typing DELETE (and a `{confirm:'DELETE'}` body server-side, so a replay or mis-click can't trigger one). **Safety, all verified against production rather than asserted:** the delete RPC holds its own allowlist of the six (table, timestamp-column) pairs and *raises* for anything else — `orders`, `customers` and a mismatched column pair were each refused live; it is service-role only (anon refused, 42501); tenant_id and cutoff are mandatory (null refused); every statement is bounded (2,000 rows per batch, 20,000 per class per run, hard cap 50,000 in SQL) and tenant-scoped. A tenant with no policy is skipped entirely, so **"keep forever" is still the default and an unconfigured VAR is never touched**. `retention_runs` audits every class of every tenant — including zero-row and failed outcomes, so "nothing to delete" and "the delete failed" never look alike — and the page renders that history. **Live end-to-end (11/11):** a temp tenant with a 90-day policy had 5 old notifications removed and 3 recent ones kept, the audit row recorded 5, an unauthenticated cron call was refused, and the **platform tenant's 12,760 rows across all six classes were byte-for-byte unchanged** before and after. Temp tenant and audit rows removed afterwards; production counts confirmed identical. 9 unit tests pin the behaviour that matters most — no policy deletes nothing, an out-of-range (typo'd) policy deletes nothing, only allowlisted targets are ever called, batches stop short or cap, and a failure is recorded rather than reported as zero.
>
> **Live chat — resolved 2026-09-18 (product decision):** the existing AI
> `ChatAssistant` is what's wanted; no human-staffed live-chat widget will be
> built. Removed from the open list and from Open decisions below.
>
> **Known weakness, needs a decision:** password policy is enforced client-side only, on both password-set flows. There is no server-side re-validation, because password changes go from the browser straight to Supabase Auth — closing it means routing them through our own API, which is an auth-flow change with real lockout risk.
>
> **2026-09-18 — four more settings were unsettable, same root cause as `allowedIps`.** The admin tenant editor renders 16 branding controls; `brandingSchema` accepted 13. Zod silently strips unknown keys, so `requireMfa`, `passwordPolicy`, `emailFromName`/`emailFromAddress`/`smsSenderId` were discarded on every save while the UI reported success. Consequences: the "Require MFA" switch could not even store its value; **the tenant password policy was permanently null, so the policy both password-set flows validate against never existed** — the client-side enforcement had nothing to enforce; and a platform admin could not set a tenant's sender identity (a VAR still could, via `/var/communications`). All four added to the schema, with `passwordPolicy.minLength` floored at 8 so a tenant can raise the platform minimum but never weaken it, and complexity rules staying opt-in. A test now compares the fields the page edits against the fields the schema accepts and fails naming any that are dropped — this exact defect has now occurred four times, so it is worth pinning.
>
> **MFA — ☑ ENFORCED 2026-09-18.** Previously: enrolment worked and Supabase already forced the step-up challenge at login for users who HAD a factor, but nothing stopped a user with NO factor from ignoring the `/profile` prompt, so `requireMfa` was a nudge, not a control. Now enforced server-side in `requireAuth()`: the session's `aal` claim is decoded locally from the JWT already in hand (no extra round-trip) and compared against `aal2`; the tenant branding row was already in that function's `select`, so there is no extra query either. Checking assurance level rather than "has a factor" covers both never-enrolled and enrolled-but-signed-in-without-the-challenge.
>
> The two design questions flagged here are resolved, not worked around:
> - *401 vs 403.* `requireAuth()` still returns `null`, so **none of the 154 call sites changed** — which matters because 10 of them call `unauthorized()` inside *synchronous* guard helpers, ruling out making it async. A WeakMap keyed by Next's per-request work-unit store records *why* the request was denied and `unauthorized()` reads it synchronously, answering **403 `{ code: 'mfa_required' }`** instead of 401 for an MFA block. *(First cut used React `cache()` for this; a probe route proved it does **not** memoize inside route handlers — only during a React render — so `unauthorized()` would have answered 401 every time. Replaced the same day and re-proved with a probe: 401 → 403 after an MFA deny → 401 on the next request, no cross-request leak. The store is a Next internal, so every access is guarded — if it ever goes away the API still blocks, it just says 401.)* A 401 reads as "session gone" and bounces to `/login`, away from `/profile` where the user enrols — so the distinct status is what keeps the remediation path reachable.
> - *The proxy fast path.* Not used, deliberately. A check there would need a per-request tenant lookup on the hot path and would be skipped whenever the routing cookies are present. Enforcement lives at the data layer instead, which no navigation can skip. `MfaRequiredBanner` (dashboard shell) supplies the explanation the proxy would have given, checks assurance level exactly as the server does, and re-checks on auth state change so enrolling clears it in place.
>
> Fails **closed**: `accessTokenAal()` returns `null` on any malformed token, so a token it cannot parse is treated as unsatisfied, never as satisfied (6 unit tests pin this). Enrolment stays reachable while blocked because `/profile` talks to the Supabase client directly and never through a guarded route. No-op until a tenant sets `requireMfa` explicitly true — which today no tenant does.
>
> **Feature flags:** 5 of 12 enforced (see Month 1). The other 7 are settable controls that currently change nothing.
>
> **2026-09-18 evening sweep — tours, settings surfaces, search.** Three checks run against the tree, not the doc:
> - **Onboarding tours:** all 12 `data-tour` targets referenced by `tours.ts` exist in the rendered components, and each of the 9 role tours (6 core + 3 VAR) targets a sidebar item that role can actually see (cross-checked against `Sidebar.tsx` roles arrays). A missing target is skipped, not stuck. In-app Help gained entries for everything shipped today.
> - **Settings-surface parity** (keys each page sends vs keys each API schema accepts — the defect class behind `allowedIps`, `requireMfa`, and friends): 15 page↔route pairs swept. One real hit: **`/admin/sla-rules` could not create a rule** — the schema required a `to_status` column that does not exist and the page never sent it, so every Create was a 400; fixed, schema now mirrors the table (verified live: 14 seeded rules, all matching). The other flags were false positives (keys sent to a different route, or nested under `limits`).
> - **RLS, every table (2026-09-19):** all 57 public tables have RLS enabled; reading each with the public anon key returned rows from exactly one — `repair_costs`, via an explicit "Anyone can read" policy. That is Byte-Back's internal repair-cost model, the pricing internals the public value-lookup deliberately withholds, listable by anyone holding the anon key that ships in the browser bundle. Policy replaced with authenticated-only (applied to production; anon now 0 rows, service role and every signed-in path unaffected — the public lookup uses the service role).
> - **The Playwright suite is green again (2026-09-22).** `auth.spec.ts` and `role-access.spec.ts` had two long-standing failures. Both were **stale tests, not app defects** — proved by building the pre-session commit (`632110c`) in a separate worktree and running the same two specs against it: identical 2 failed / 15 passed, so nothing in this session caused them. (1) The logout test clicked `getByTitle('Sign out')`; the sidebar control was renamed to "Log out", so the locator matched nothing — logout itself works. (2) The vendor access test allowed only `/` or `/dashboard` after a denied route, but the proxy sends a denied user to `/`, which then forwards them to *their own* landing — `/vendor/orders` for a vendor. The app had denied the route correctly. Both tests fixed; **17/17 pass.**
> - **Un-gated pages actually render (2026-09-22):** the 11 pages un-gated on 09-18 had rendered `<ComingSoon />` for months, so their real implementations had never executed in a browser — a runtime error inside one is invisible to `tsc` and `next build`, which only compile them. `tests/e2e/ungated-pages.spec.ts` now mounts all 18 admin/VAR pages as a signed-in admin against a production build and fails on a page error, an error boundary, an empty render, or leftover "Coming Soon" text. **18/18 pass.**
> - **Search:** `sanitizeSearchInput` stripped dots, so any email/domain search across all 14 search callers returned 0 rows (proved live). Fixed.
- ❓ **NEW: "Restart services" (BB Admin Support) doesn't map to a serverless architecture.** The platform runs on Vercel (no persistent server process to restart). Needs clarification on actual intent — redeploy? clear a specific cache? restart a background/cron job?
- ❓ **NEW: "Encryption keys" (BB Admin Security) — app-layer feature or infra-level (Supabase-managed)?** Not clearly an application feature to build; likely already handled at the infrastructure layer. Confirm before scoping.
- ☑ **Data retention policies** — policy + dry-run report (2026-09-18) and live scheduled execution with per-run audit (2026-09-22). See the Month 4 status note.

*3.5-mo: auth enforcement + impersonation + launch are fixed; API/public-API and vendor auction move to a post-launch fast-follow.*

---

## Full source-outline audit (every section, 2026-08-17)
Every section of `BB VAR Outline structure revised 2.0.docx`, checked against
the live codebase line by line. Anything not called out here matches the plan
above with no new finding. ✅ built · ◐ partial · ☐ not built · ❓ needs clarification

**Platform look & feel** — ✅ white background / blue-on-blue Byte-Back mark (light-mode login fix + rebrand, shipped).

**Guiding principle** (Admin controls platform / VAR controls own business / End Customer controls own company / VARs invisible to each other) — ✅ tenant isolation + RLS. **Exception: ☐ the email/PDF branding leak in Critical findings #1** — this principle is violated today for every outbound email and PDF.

**Revenue model** (platform commission % + product margin %, admin-adjustable input fields, blended not itemized, BB-only reporting split) — ✅ commission engine + reporting shipped; ❓ deal-by-deal per-order adjustment vs. tenant-default only — not directly verified; **☐ QA item added to Month 3** to confirm PDFs never itemize BB's cut.

**VAR revenue model** (Corp Tab / Rep Tab margin input fields) — ✅ `corpMargin`/`repMargin` exist in the commission config (`MarginSpec`, seen in `var/page.tsx`).

**1. Platform Admin** —
- Create/Edit/Delete/Suspend/Activate VARs — ✅ `admin/tenants` CRUD page exists.
- Set white-label domains — ◐ operational via runbook, not a one-click admin UI field.
- Configure branding — ◐ exists but limited (see logo/secondary-color gaps in Month 4).
- Configure licensing / subscription plans — ✅ `admin/plans`.
- Configure storage quotas — ✅ `license.storageMb` enforced on upload against measured bucket usage (2026-09-19).
- Configure feature availability — ✅ ceiling exists (`features.ts`); ❓ VAR-facing toggle within that ceiling (Month 2 new item).
- Configure API integrations / SMTP / SMS / payment gateways — ◐ per-tenant sender identity (From name/address, SMS sender ID) ✅ on branding and consumed by the send path; public API ✅ (2026-09-18); payment gateway ☐ deferred to the client face-to-face.
- **BB User Management "is this role different from Platform Management? Can we combine both?"** — this was an open question **in the source doc itself**, not from the client. Current build already resolves it: one `admin` role does both. No action needed unless you want them split.
  - Create Admins / Create VAR Administrators — ✅ platform admins via the Users page; **VAR administrators via the VAR page's "VAR administrators" card (shipped 2026-09-18)** — see Month 1.
  - Reset passwords / Lock accounts / Delete users — ✅.
  - Assign permissions — ☐ blocked on the RBAC gap (Critical finding #3).
  - View login history — ❓ `audit_logs` exist; a dedicated login-history view not directly confirmed.

**BB Customer Management / Model** (VARs = Telco Reps, Distributors like TD Synnex, Resellers like Evergreen; BB's own direct customers sit under a "BB VAR" category) — ✅ matches the platform-tenant (`PLATFORM_TENANT_ID`) design already built.

**BB Admin Platform "View ALL"** (VARs, Customers, Devices, Tickets, Reports, Billing, Pricing, Audit Logs, Activity) — ✅ all present in the admin nav.

**BB Admin Pricing** (pricing tables, deal-by-deal override, depreciation table for RVE, communicate pricing discrepancies to VAR) — ✅ pricing admin + RVE depreciation table exist; ◐ discrepancy notice exists **per-order** (`notify-price-change`), not as a VAR-wide "pricing table changed" broadcast — different granularity than the outline implies, worth a quick confirm.

**BB Admin Billing** (invoice VARs, manage subscriptions, credits/reconciliation, payment history, refunds, taxation, revenue + commission/holdback reports) — ✅ mostly shipped; ◐ `invoice_payments` (payments + refunds) exists at the schema+API level (found in this audit) but needs a UI confirmation pass; ☐ the reconciliation step itself (period → invoice line items) is the real remaining gap, already tracked in Month 3.

**BB Admin Reporting** (Global/Revenue/MRR/ARR/Active-Inactive/Devices/Usage/Storage/API usage/Licenses/Security/Failed logins) — ◐ MRR/ARR, usage (orders MoM), active/inactive, per-VAR licenses all real (Month 3 suite, 2026-08-25); storage (measured) + API calls + AI tokens (metered, MTD) ✅ since 2026-09-19; failed-login metric intentionally absent (auth is client→Supabase, no server path records attempts).

**BB Admin Support** (impersonate VAR/customer, reset accounts, force password reset, view/export logs, restart services) — ✅ impersonation shipped 2026-08-27 (session swap + banner, admin-only, can't target another admin); logs exist; ❓ "restart services" (Month 4 new item).

**BB Admin Security** (roles, permissions, SSO, MFA, API keys, encryption keys, audit logs, IP restrictions, password policy, retention policies) — *(re-verified 2026-09-18)* ◐ roles/audit shipped; IP restrictions ✅ genuinely enforced, and now format-validated so a typo can't lock a whole tenant out; API keys ✅ — management UI plus the read-only public API at `/api/v1` (shipped 2026-09-18, verified end-to-end against production); permissions ◐ catalog/guard infra exists, used by one route, and the `permissions`/`role_permissions` tables are seeded but read by nothing; **password policy ◐ — the "zero callers" note was wrong**, it is enforced on both password-set flows, but client-side only, with no server-side re-validation; **MFA ✅ enforced 2026-09-18** in `requireAuth()` (aal2 assurance check, 403 `mfa_required`, fails closed — see the Month 4 status note); retention policies ✅ (per-tenant policy + dry-run report 2026-09-18; live nightly execution + audit 2026-09-22); ☐ SSO; ❓ encryption keys (Month 4 new item).

**2. VAR Administrator** ("should feel like they own the software," never see another VAR, everything tenant-limited) — ✅ matches tenant isolation exactly.

**VAR White-Label Settings** — *(re-verified against code 2026-09-18; five entries here were stale)* logo upload ✅; Corp/Rep margin ✅; company name ✅; primary color ✅; **secondary color ✅** (the field DID exist — it just drove a `--brand-secondary` CSS var that nothing read, so the admin's colour picker previewed an effect the app never applied; it now drives the outbound email header gradient); login page ☐ *(the one genuine gap — the login page is tenant-branded, but there's no per-VAR layout/content)*; email/notification templates ✅ **but only since 2026-09-13** — the 2026-08-23 claim was wrong, `resolveWhiteLabel` had no consumers at all until then; support email ✅; **support phone ✅** (`TenantBranding.supportPhone`, rendered on the login page); **KB links ✅** (returned by `GET /api/kb`, surfaced on the reader page); custom domain ✅ (operational); **privacy policy ✅** (`/privacy-policy` redirects to the tenant's own URL when set).

**VAR Customer Management** — create/suspend/move/search/bulk-import/bulk-export ✅; assign licenses/plans **per customer** ✅ (shipped 2026-08-23, `customers.plan_id`); archive as a distinct state — resolved, suspend covers it (see Month 2); automated reminders under VAR letterhead — ◐ the reminder email exists (`sendRecurringTradeInReminderEmail`) but **inherits the same Critical finding #1 branding leak** — it goes out under Byte-Back branding today regardless of the VAR.

**VAR User Management** (create customer users/admins, disable, reset passwords, force MFA, assign permissions, add/disable reps) — this **is** M2.3, shipped. The "force MFA" piece is ✅ as of 2026-09-18, at tenant granularity: an operator sets `requireMfa` on the tenant and `requireAuth()` blocks every single-factor session in it. Per-*user* forcing (require MFA of one rep but not another) is not built and is not in the outline.

**VAR Billing** — ✅ both options shipped 2026-09-22, chosen per VAR (`settings.billingMode`): Option A (bill outside the platform, the default) and Option B (in-platform customer invoices with regional tax, manual payment recording, outstanding-balance tracking). Card collection awaits the payment-gateway decision.

**VAR Reporting** (roll-up of all reps, view by rep/region, manage commissions) — this **is** M2.3b, next after M2.3.

**VAR Product Configuration** (enable/disable features, API keys, custom workflows, automation, alerts, notification settings) — ✅ feature toggle shipped; ✅ VAR-facing API key management (2026-08-27) + read-only public API `/api/v1` (2026-09-18) — keys now unlock orders/customers/devices for the key's tenant, gated by the `api_access` feature; ☐ custom workflows/automation (not scoped anywhere yet — flag for a future phase, out of the current 4-month window unless reprioritized); ◐ alerts exist internally (SLA) but not VAR-configurable; notification settings exist per-user, not per-VAR-tenant broadly.

**VAR Support** (tickets ✅; KB ✅ shipped 2026-08-27; chat ✅ — **resolved 2026-09-18: the AI `ChatAssistant` is what's wanted**, no human live-chat widget to build).

**VAR Security** (customer audit logs / login history — ◐ infra exists, scoped view not confirmed; API tokens ✅ management + consuming `/api/v1` (2026-09-18); customer SSO ☐; password policy "within allowed limits" — ◐ enforced on both password-set flows against the tenant's `branding.passwordPolicy`, client-side only, and set by the platform admin rather than the VAR — the "called from nowhere" note here was stale, see Month 4).

**VAR Restrictions** (cannot see other VARs / global revenue / alter BB pricing / change licensing or platform settings / delete platform data / manage admin users / modify billing or integrations engines) — ✅ believed enforced throughout by role checks; every admin-only route read during this audit gates strictly on `role === 'admin'`.

**Multi-level VAR flexibility note + Appendix A** (both worked examples: 3-tier "Sales Co." and 5-tier "Bell") — ✅ **confirmed the current 3-role delegation model (`var_entity_admin` → `var_regional_manager` → `var_sales_rep`) already satisfies both given examples** — the shallower example simply doesn't use the regional tier. The outline's "need flexibility for more levels" is framed as a future ask, not required for either worked example.

**3. End Customer** — dashboard (usage/devices/reports/alerts/notifications/storage/licenses) — ◐ an orders-focused dashboard exists; the broader usage/storage/license view does not yet (depends on Month 1 metering work).

**Customer User Management** (create/disable users, reset passwords, assign permissions, invite, delete) — ✅ already works via the existing org-admin pattern (`/customer/team`).

**Customer Company Settings** (profile, locations, departments, contacts, hours, notifications) — ✅ shipped 2026-08-17 (this session); notifications piece is the pre-existing separate per-user preference system, not part of `company_profile` — same end result, different field.

**Customer Data Management** (import/export/search/reports/history) — ✅ order-level import/export/search; asset change history ✅ (2026-08-23); asset bulk import ✅ (`/api/customer/assets/bulk`, 2026-08-23 — the "single-row only" note here was stale).

**Customer Devices/Assets** (register/assign/retire/move/audit/track) — ✅ shipped, "audit" (change-history log) landed 2026-08-23.

**Customer Reports** (operational/compliance/inventory/financial/user-activity/device-history/exports) — ◐ order + asset roll-up counters, order history table, recent-assets table, and CSV/PDF export shipped 2026-08-23 (`/customer/reports`); no dedicated compliance/financial report types beyond that.

**Customer Support** (tickets ✅; KB ✅ shipped 2026-08-27; chat ✅ — same resolution as VAR Support above: the AI assistant satisfies it).

**Customer Security** (MFA ✅ — enrolment on `/profile` plus tenant-level enforcement in `requireAuth()` as of 2026-09-18; password changes ✅; API tokens ☐; own audit log ❓ not confirmed customer-facing; IP restrictions — not customer-facing by design; SSO if licensed ☐).

**Customer Restrictions** — ✅ believed enforced architecturally (tenant + org scoping throughout); no new gap found.

**Permission Matrix** (the full Platform Admin / VAR Admin / Customer Admin table) — kept as the reference target; fully realizing it row-by-row requires the RBAC permission-string model (Critical finding #3) — right now enforcement is coarser (role-level, not the fine per-function grid the matrix describes).

**Enterprise Best Practices** (7 recommendations) — RBAC granular ◐ (catalog + guard infra built 2026-08-27, adopted by zero real routes yet — not the same as "done"); multi-tenant ✅; feature flags ✅ (VAR self-toggle shipped); audit logging ✅; delegated administration ✅ (roles + self-service rep creation both shipped); secure impersonation ✅ shipped 2026-08-27 (log + session-swap + banner); scalable licensing ◐ (customers/users/plans/storage/API calls yes — storage + API since 2026-09-19; transactions per month has no runtime source).

---

## Cross-cutting (every phase)
- gstack workflow: small self-verifying commits, build→test→fix loop, keep the tree green
- Keep it **additive**; each change verified with `tsc` + vitest (baseline unchanged) + production build for risky/page work
- Update this doc's checkboxes as phases land; keep [[BB_VAR_GAP_MAP]] in sync

## Open decisions (need product input)
- ~~Vendor auction in-platform?~~ **Answered 2026-08-17: not initially — revisit complexity for a future phase.** See Month 4 note for the complexity read.
- Custom domains vs subdomains per VAR — pros/cons written up and sent to client 2026-08-17 (Telus example); awaiting their choice per-VAR (can be mixed — doesn't need to be all-or-nothing).
- ~~**Billing Option A vs B**~~ **Answered 2026-09-22: BOTH, selectable per VAR.** Option A stays the default; Option B is built and live-tested (see Critical finding #2). Payment collection within Option B still waits on the gateway decision below.
- Tax model confirmation (province table shipped) — outstanding.
- Payment gateway choice — **client deferred to the face-to-face (Weeks 9–12 discussion).**
- **NEW: SMTP/SMS/payment-gateway configuration — who configures it?** Outline places this under BB Admin, not VAR settings, suggesting Byte-Back staff sets a VAR's sender identity from an admin panel rather than the VAR self-configuring provider credentials. Confirm with client — changes whether Month 4's "Integrations: SMS/SMTP config UIs" needs a VAR-facing UI at all, or only a BB-admin one.
- ~~**NEW: "live chat" — human support chat, or is the existing AI ChatAssistant sufficient?**~~ **Answered 2026-09-18: the AI assistant is sufficient.** No human-staffed live-chat widget will be built. "Chat" under VAR Support and Customer Support is satisfied by the shipped `ChatAssistant`; escalation from chat to a ticket remains the path to a human.
- **NEW: "Archive customers" as a distinct state from "Suspend customers"** — confirm if this needs its own status or is just alternate wording (Month 2 finding). Resolved 2026-08-23: alternate wording — suspend already covers list-hiding, order-blocking, login-blocking, and restore; see Month 2 bullet for evidence.
- **NEW: per-order commission adjustment** — confirm whether admin needs to override the commission % per individual deal, or a tenant-wide default is sufficient (Revenue Model section finding).
