// ============================================================================
// ADMIN PRICING — paused from release
// ============================================================================
// The full implementation lives in ./_client (unchanged). Re-enable by
// restoring the dynamic import of AdminPricingClient as the default export.

'use client'

import { ComingSoon } from '@/components/ComingSoon'

export default function AdminPricingPage() {
  return <ComingSoon title="Pricing" />
}
